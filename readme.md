# 🏙️ Urban Amenity Extractor
A GeoSpatial data pipeline to audit local infrastructure.

## 🛠️ Setup
1. Install: `pip install -r requirements.txt`
2. Add your key to `.env`
3. Run: `python src/main.py`